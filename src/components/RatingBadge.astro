---
// Star rating badge. Driven by business.rating in src/data/site.ts.
// Only show a rating that matches the real public profile.
import { business } from '../data/site';
interface Props { class?: string }
const { class: className = '' } = Astro.props;
const { stars, source, url } = business.rating;
const Tag = url ? 'a' : 'div';
const linkProps = url ? { href: url, target: '_blank', rel: 'noopener' } : {};
---
<Tag
  {...linkProps}
  class:list={['glass inline-flex items-center gap-3 rounded-full py-2 pl-2 pr-4', url && 'transition-transform duration-500 hover:-translate-y-0.5', className]}
  aria-label={`Rated ${stars} out of 5 on ${source}`}
>
  <span class="flex h-8 w-8 items-center justify-center rounded-full bg-white shadow-[var(--shadow-soft)]" aria-hidden="true">
    <!-- Google "G" -->
    <svg class="h-4 w-4" viewBox="0 0 24 24"><path fill="#4285F4" d="M23.5 12.3c0-.8-.1-1.6-.2-2.3H12v4.4h6.5a5.6 5.6 0 0 1-2.4 3.6v3h3.9c2.3-2.1 3.5-5.2 3.5-8.7Z"/><path fill="#34A853" d="M12 24c3.2 0 6-1.1 8-2.9l-3.9-3c-1.1.7-2.5 1.2-4.1 1.2-3.1 0-5.8-2.1-6.7-5H1.3v3.1A12 12 0 0 0 12 24Z"/><path fill="#FBBC05" d="M5.3 14.3a7.2 7.2 0 0 1 0-4.6V6.6H1.3a12 12 0 0 0 0 10.8l4-3.1Z"/><path fill="#EA4335" d="M12 4.8c1.8 0 3.3.6 4.6 1.8l3.4-3.4A12 12 0 0 0 1.3 6.6l4 3.1c.9-2.9 3.6-4.9 6.7-4.9Z"/></svg>
  </span>
  <span class="flex items-center gap-0.5" aria-hidden="true">
    {[1, 2, 3, 4, 5].map((n) => (
      <svg class:list={['h-4 w-4', n <= stars ? 'text-[#f5b301]' : 'text-navy/15']} viewBox="0 0 20 20" fill="currentColor"><path d="M10 1.5l2.6 5.3 5.9.9-4.3 4.1 1 5.8L10 14.9l-5.2 2.7 1-5.8L1.5 7.7l5.9-.9L10 1.5Z"/></svg>
    ))}
  </span>
  <span class="text-sm font-semibold text-navy">{stars.toFixed(1)} on {source}</span>
</Tag>
